# Rust
target/
*.rlib
*.rmeta
# Cargo.lock is intentionally checked in — this workspace ships a binary
# (`aresadb-cluster`) and a Docker image that both need reproducible
# builds. Downstream library consumers still get semver-compatible
# resolution via `cargo update` on their side.

# Benchmarks
benchmark_results/

# Python
.venv/
__pycache__/
*.pyc
*.pyd
*.so
*.egg-info/
dist/
python/.venv/
python/target/

# Environment
.env
.env.*
!.env.example

# OS
.DS_Store
._*

# IDE
.vscode/
.idea/
*.swp
*.swo

# Data
*.parquet
*.csv.gz

# Logs
*.log

# Docker
docker-compose.override.yml

# Demo database (generated)
demo_db/data/
